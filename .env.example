# Milestone D+ (not needed for Milestone A — the mandate chain runs with ephemeral keys)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=
ANTHROPIC_API_KEY=

# Signing keys (JWK JSON strings). If unset, ephemeral keys are generated per process.
BUYER_AGENT_JWK=
MERCHANT_JWK=
PLATFORM_JWK=
